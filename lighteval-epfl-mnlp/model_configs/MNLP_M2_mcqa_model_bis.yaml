model:
  base_params:
    model_args: "pretrained=NicoHelemon/MNLP_M2_mcqa_model_bis,revision=main,trust_remote_code=true"
    dtype: "float16"
    compile: false

  # Ignore this section, do not modify!
  merged_weights:
    delta_weights: false
    adapter_weights: false
    base_model: null
  generation:
    temperature: 0.0